# NovelOS Agent权限协议 v1.0
Agent:
  总导演: {允许: [读取规划, 读世界状态, 总线调度], 禁止: [直接改数据, 直调API, 操作上下文]}
  世界: {允许: [读写世界引擎]}
  角色: {允许: [读写角色引擎]}
  剧情: {允许: [读写剧情引擎, 注册伏笔]}
  爽文: {允许: [调用爽感算法, 给剧情提建议]}
  审核: {允许: [读文本, 标记违规]}
  修复: {允许: [基于审核请求重生成]}
通用禁止: [绕过20直调21, 读其他Agent内部, 未经授权启动长期任务]
